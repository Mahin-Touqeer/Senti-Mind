import { Zoom } from "react-toastify";

export const toastOptions = {
  position: "top-right",
  autoClose: 1000,
  hideProgressBar: true,
  closeOnClick: true,
  draggable: false,
  theme: "dark",
  transition: Zoom,
  className: "alink",
};
